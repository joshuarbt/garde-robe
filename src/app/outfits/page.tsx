import { PageShell } from "@/components/layout/PageShell";

export default function OutfitsPage() {
  return (
    <PageShell
      title="Outfits"
      description="Saved looks and the outfit canvas will live here. Drag items onto a blank canvas to compose outfits."
    />
  );
}
